<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    >
    <data>
        <variable
            name="viewModel"
            type="com.example.along.scmusic.screen.seemore.ItemSeeMoreMusicViewModel"
            />
    </data>
    <android.support.v7.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/dp_02"
        app:cardCornerRadius="@dimen/dp_03"
        app:cardUseCompatPadding="true"
        >
        <ImageView
            android:id="@+id/image_track"
            android:layout_width="match_parent"
            android:layout_height="@dimen/dp_180"
            android:scaleType="fitXY"
            app:url="@{viewModel.mTrackObservableField.artworkUrl}"
            tools:ignore="ContentDescription"
            />
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom"
            android:background="@color/transparent_color_black"
            android:gravity="center"
            android:maxLength="13"
            android:padding="@dimen/dp_07"
            android:text="@{viewModel.mTrackObservableField.title}"
            android:textColor="@android:color/white"
            android:textSize="@dimen/sp_14"
            android:textStyle="bold"
            />
    </android.support.v7.widget.CardView>
</layout>
